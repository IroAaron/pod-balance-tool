rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // No auth yet — internal tool, small trusted team. Revisit if this becomes a problem.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
